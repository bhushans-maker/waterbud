name: Build Android APK

on:
  push:
    branches: [ main, master, develop ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js (v24)
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Find Project Directory
        id: find_project
        run: |
          PROJECT_DIR=$(find . -name "package.json" -not -path "*/node_modules/*" -exec dirname {} \; | head -n 1)
          if [ -z "$PROJECT_DIR" ]; then
            echo "::error::Could not find package.json."
            exit 1
          fi
          echo "project_dir=$PROJECT_DIR" >> $GITHUB_OUTPUT

      - name: Install dependencies
        working-directory: "${{ steps.find_project.outputs.project_dir }}"
        run: npm install --no-package-lock

      - name: Build Web Assets
        working-directory: "${{ steps.find_project.outputs.project_dir }}"
        run: npm run build

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Android SDK Setup
        uses: android-actions/setup-android@v3

      - name: Repair Gradle
        working-directory: "${{ steps.find_project.outputs.project_dir }}/android"
        run: |
          rm -f gradle/wrapper/gradle-wrapper.jar
          gradle wrapper

      - name: Capacitor Sync
        working-directory: "${{ steps.find_project.outputs.project_dir }}"
        run: npx cap sync android

      - name: Generate App Assets
        working-directory: "${{ steps.find_project.outputs.project_dir }}"
        run: |
          if [ -f "resources/icon.png" ]; then
            npx @capacitor/assets generate --android
          else
            echo "No resources/icon.png found, skipping asset generation"
          fi

      - name: Build APK File
        working-directory: "${{ steps.find_project.outputs.project_dir }}/android"
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Save APK to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: PaniMitr-App-APK
          path: "**/*.apk"
